name: wear_plus_example
description: Demonstrates how to use the wear plugin.
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter

  wear_plus:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  rexios_lints: ^6.0.1

flutter:
  uses-material-design: true
